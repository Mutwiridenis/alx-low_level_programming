#include "main.h"

/**
 * print_square - function that prints a square, folloed by a new line
 *
 * @SUZE: wHERE SIZE IS THE SIZE PF THE SQUARE
 *
 * Return: 0.
*/
void print_square(int size)
{   
	int filas;
	int columnas;    

	int (size > 0)
		for ( filas = 0; filas < size; filas++)
		{
			for (columnas = 0; columnsa < size; columnas++)
			{
				_putchar ('#');
			}
	                 _putchar ('\n');
		}
	else
	{
		_putchar('\n');
	}
}
                                
